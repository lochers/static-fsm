[package]
name = "fsm_macro"
version = "0.1.0"
edition = "2021"

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = "2.0"

[dev-dependencies]
fsm = { version = "0.1", path = "../fsm" }

[lib]
proc-macro = true
